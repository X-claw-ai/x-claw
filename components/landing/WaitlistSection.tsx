// DEPRECATED, single-product narrative pushes users straight to /launch
// instead of a waitlist. Empty export so old imports keep compiling.
export default function WaitlistSection() {
  return null;
}
