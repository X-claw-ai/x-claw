// DEPRECATED, landing now ends on the Waitlist (early access) section.
// Roadmap content moved into VisionSection / AgentVerticalsSection. Kept as
// an empty export so existing imports do not break.
export default function RoadmapSection() {
  return null;
}
