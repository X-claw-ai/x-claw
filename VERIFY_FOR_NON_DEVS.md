# KOKi.ai — 비개발자용 검증 가이드 (30분, 터미널 안 씀)

KOKi.ai가 진짜 동작하는지 직접 눈으로 보고 싶으신 분을 위한 단계별 가이드.
**터미널 / 코드 / git 명령어 전혀 필요 없음.** 모든 게 웹 브라우저에서 끝납니다.

준비물:
- 컴퓨터 (Mac, Windows, Linux 다 OK)
- 인터넷
- 이메일 주소 1개 (GitHub + Vercel 가입용)

비용: **0원** (Vercel 무료 플랜으로 충분)

---

## 전체 흐름

```
1. 코드 다운로드   →  2. GitHub에 업로드   →  3. Vercel로 배포   →  4. 브라우저로 확인
   (1분)              (10분)                  (10분)              (5분)
```

배포가 성공하면 = **코드가 잘 컴파일되고 잘 돌아간다**는 뜻이에요. Vercel이 자동으로 모든 의존성 설치 + 빌드 + 배포까지 해주니까, 우리가 한 일이 진짜 작동하는지 확인하는 가장 쉬운 방법입니다.

---

## 1단계: 코드 다운로드 (1분)

이 대화창에서 받은 `koki.zip` 파일을 컴퓨터에 다운로드.

받은 zip을 더블클릭해서 압축 해제. `koki/` 폴더가 나옵니다 (안에 138개 파일).

✅ 체크: 폴더 안에 `package.json`, `README.md`, `app/`, `components/` 같은 게 보이면 OK.

---

## 2단계: GitHub 가입 + 코드 업로드 (10분)

### 2-1. GitHub 가입 (없으면)

https://github.com/signup 에서 이메일로 가입. 이메일 인증까지 끝내야 함.

### 2-2. 새 repo 만들기

로그인 후:

1. 우측 상단 `+` 아이콘 → **New repository** 클릭
2. **Repository name**: `koki`
3. **Public** 선택 (오픈소스이니까)
4. **다른 옵션은 모두 빈 채로** (README, .gitignore, license 추가 안 함 — 우리 zip에 이미 있음)
5. 맨 아래 **Create repository** 클릭

### 2-3. 파일 업로드

repo가 만들어지면 빈 페이지가 보입니다. 거기에:

1. 페이지 중앙의 **uploading an existing file** 링크 클릭
   (또는 `https://github.com/<당신아이디>/koki/upload/main` 으로 직접 가도 됨)
2. **압축 해제한 `koki/` 폴더 안의 모든 항목**을 드래그해서 업로드 영역에 떨어뜨림
   - 폴더 자체를 통째로 드래그해도 GitHub이 안에 있는 파일 다 인식함
   - **주의**: `koki/` 폴더 자체가 아니라 그 **안의 내용**을 올려야 함 (그래서 repo 루트에 `package.json` 보이게)
3. 파일 다 올라가면 페이지 아래로 스크롤
4. **Commit changes** 버튼 클릭 (메시지 박스는 건드릴 필요 없음)

✅ 체크: repo 메인 페이지에 가면 `app/`, `components/`, `lib/`, `package.json`, `README.md` 등이 보여야 함.

---

## 3단계: Vercel 배포 (10분)

### 3-1. Vercel 가입

https://vercel.com/signup 으로 이동.

**Continue with GitHub** 클릭 → GitHub 계정으로 로그인. 권한 요청 다 허용.

### 3-2. 새 프로젝트 만들기

1. Vercel 대시보드 좌측 위 **Add New...** → **Project**
2. **Import Git Repository** 섹션에서 방금 만든 `koki` repo 옆 **Import** 클릭
3. **Configure Project** 화면이 뜸:
   - **Framework Preset**: Next.js (자동 감지됨, 그대로)
   - **Root Directory**: `./` (그대로)
   - **Build Command**: `npm run build` (그대로)
4. **Environment Variables** 섹션 펼치기 — **여기가 중요**:

| Key | Value | 어디서 받음 |
|---|---|---|
| `XAI_API_KEY` | (xAI에서 발급받은 키, 예: `xai-abc...`) | https://console.x.ai → API Keys |
| `NEXT_PUBLIC_SOLANA_RPC_URL` | `https://api.mainnet-beta.solana.com` | 그대로 입력 (또는 Helius/QuickNode 유료 RPC) |

   - 키 없으면 일단 비워둬도 배포는 됨. 다만 Grok 호출하는 기능들은 mock으로 동작.
   - 키는 나중에 Vercel 프로젝트 설정에서 추가/수정 가능.
5. **Deploy** 버튼 클릭

### 3-3. 빌드 대기 (2~3분)

Vercel이 자동으로:
- npm install (모든 패키지 설치)
- npm run build (Next.js + TypeScript 컴파일)
- 배포

화면에 빌드 로그가 흐릅니다. 끝까지 다 통과하면:
- **🎉 Congratulations!** 페이지가 뜨고 `https://koki-xxx.vercel.app` 같은 주소가 나옵니다.

❌ **만약 빌드 실패하면**: 빨간 글씨로 에러가 표시됩니다. 그 에러 메시지 그대로 저한테 보여주시면 바로 패치합니다.

✅ **빌드 성공 = 코드가 진짜 잘 컴파일되는 상태입니다**.

---

## 4단계: 브라우저로 확인 (5분)

Vercel이 준 주소(`https://koki-xxx.vercel.app`)로 이동해서 페이지마다 클릭해보세요.

### 체크리스트

- [ ] **`/` 메인 페이지** — Hero, Attention Layer, Engines, Safety, Final CTA 5섹션 다 보임
- [ ] **상단 메뉴 Dashboard 클릭** → `/dashboard`
- [ ] 대시보드 최상단에 **Real-time Meme Radar** + 4개 카드 (Shibatoshi, Grok Cat, Based Frog, NPC Whale) 보임
- [ ] 카드의 **Generate Launch Kit** 버튼 클릭 → `/launch?meme=shibatoshi` 진입
- [ ] **Launch 페이지에서 폼이 자동으로 채워져 있음** (토큰 이름 = Shibatoshi, 티커 = SHIBATOSHI 등)
- [ ] 상단에 "Radar signal: Shibatoshi · Trend 94 · High readiness" 같은 노란 배너
- [ ] **"Generate Launch Kit" 버튼 클릭** (XAI_API_KEY 입력했으면 진짜 Grok이 호출됨, 안 했으면 mock)
- [ ] Step 2 Review 화면에 token name, ticker, 10 tweets, 20 raid replies, Telegram 공지 등 다 표시됨
- [ ] 상단에 "Generated by Grok · grok-2-latest" 또는 "Generated by mock — set XAI_API_KEY for Grok" 정직한 뱃지

위 항목 다 통과 = **프론트엔드 + LLM 호출까지 모두 정상**.

### 진짜 토큰 발행 (선택, 위험)

여기까지 통과했으면, 메인넷 토큰 발행도 가능한 상태입니다. 다만 **실제 SOL이 들어가는 일**이라 초보자에겐 권장 안 함:

- Phantom 지갑 설치 (https://phantom.app)
- 최소 0.05 SOL 입금
- Connect Wallet → Sign & Launch
- **첫 시도는 throwaway 토큰**으로 (티커 `CLWTST`, dev buy 0 SOL)

---

## 자주 깨지는 지점 + 해결

| 증상 | 원인 | 해결 |
|---|---|---|
| GitHub 업로드 시 "파일이 너무 많다" | 한 번에 100+ 파일 안 됨 | 폴더별로 나눠서 여러 번 업로드 (`app/` 먼저, `components/` 다음...) |
| Vercel 빌드 에러: `Cannot find module '@solana/...'` | 의존성 못 설치함 | `package.json`이 repo 루트에 있는지 확인. 그래도 안 되면 에러 메시지 저한테 |
| Vercel 빌드 에러: TypeScript 에러 | 코드의 type 이슈 | 에러 메시지 그대로 저한테 보여주세요. 보통 1줄 패치로 끝남 |
| 배포는 됐는데 페이지 깨짐 | env var 누락 | Vercel 프로젝트 설정 → Environment Variables에서 키 추가 후 redeploy |
| `Generated by mock` 뱃지만 뜸 | XAI_API_KEY 안 넣음 | console.x.ai에서 키 발급 후 Vercel env에 추가 |

---

## TL;DR

1. zip 다운로드 → 압축 해제
2. github.com → 새 repo `koki` → 파일 드래그 업로드 → Commit
3. vercel.com → Import → 환경변수 (XAI_API_KEY 권장) → Deploy
4. 받은 vercel.app 주소에서 메인 페이지 + Dashboard + Meme Radar 동작 확인

**여기까지 = 코드가 진짜 동작하는 상태 검증 완료.** 발표용 데모로도 그대로 사용 가능 (vercel.app URL을 트윗에 넣으면 끝).

문제 생기면 에러 메시지/스크린샷 보내주세요. 바로 잡아드립니다.
